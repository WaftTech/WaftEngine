/*
 * CodeVerifyPage Messages
 *
 * This contains all the text for the CodeVerifyPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CodeVerifyPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CodeVerifyPage container!',
  },
});
