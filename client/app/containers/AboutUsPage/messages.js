/*
 * AboutUsPage Messages
 *
 * This contains all the text for the AboutUsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AboutUsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AboutUsPage container!',
  },
});
