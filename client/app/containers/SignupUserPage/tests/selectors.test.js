// import { selectSignupUserPageDomain } from '../selectors';

describe('selectSignupUserPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
