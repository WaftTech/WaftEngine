// import { fromJS } from 'immutable';
// import { selectOrganizationDetailPageDomain } from '../selectors';

describe('selectOrganizationDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
