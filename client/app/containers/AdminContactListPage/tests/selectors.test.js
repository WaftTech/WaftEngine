// import { selectAdminContactListPageDomain } from '../selectors';

describe('selectAdminContactListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
