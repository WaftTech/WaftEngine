/*
 * LoginAdminPage Messages
 *
 * This contains all the text for the LoginAdminPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LoginAdminPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LoginAdminPage container!',
  },
});
