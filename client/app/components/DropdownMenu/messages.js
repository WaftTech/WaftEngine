/*
 * DropdownMenu Messages
 *
 * This contains all the text for the DropdownMenu component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DropdownMenu';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DropdownMenu component!',
  },
});
