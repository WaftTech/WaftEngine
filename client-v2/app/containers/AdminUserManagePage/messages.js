/*
 * AdminUserManagePage Messages
 *
 * This contains all the text for the AdminUserManagePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AdminUserManagePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdminUserManagePage container!',
  },
});
