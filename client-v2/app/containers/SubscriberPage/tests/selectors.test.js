// import { selectSubscriberPAgeDomain } from '../selectors';

describe('selectSubscriberPAgeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
