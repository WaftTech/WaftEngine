// import { selectLoginAdminPageDomain } from '../selectors';

describe('selectLoginAdminPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
